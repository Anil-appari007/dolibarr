#
name: DOLIBARR_DEPLOY_TO_ACI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    # - uses: actions/checkout@v3
    - name: docker-check
      run:  echo ${{secrets.DOCKERCONTEXT}} | docker context import dolibarr-docker-server -
      
    - name: check1
      run: docker context use dolibarr-docker-server && docker ps

