#
name: DOLIBARR_DEPLOY_TO_ACI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    # - uses: actions/checkout@v3
    - name: docker-check
      run:  echo ${{secrets.dockercontext}} > my-remote-server.dockercontext && tar cvf my.tar my-remote-server.dockercontext && docker context import dolibarr-docker-server my.tar
    - name: check1
      run: docker context use dolibarr-docker-server && docker ps

